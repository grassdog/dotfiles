# Install command-line tools using Homebrew
# Usage: `brew bundle Brewfile`

# Make sure we’re using the latest Homebrew
update

# Upgrade any already-installed formulae
upgrade

install wget --enable-iri

install git
install mercurial
install vcprompt

install ctags
install tree
install ack
install the_silver_searcher

install leiningen
install imagemagick
install multimarkdown
install pstree
install node
install ruby-build
install chruby
install pow
install z

install mongodb

install vim --with-ruby --with-mzscheme --with-python --override-system-vi
install macvim
install emacs --cocoa --srgb

install ruby-build
install chruby
install s3cmd

install xctool
install youtube-dl

linkapps

# Remove outdated versions from the cellar
cleanup
